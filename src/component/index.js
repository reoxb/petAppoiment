import React from "react";
import ReactDOM from "react-dom";
import MainInterface from "../src/MainInterface.js";

ReactDOM.render(
        <MainInterface/>, 
        document.getElementById("petAppointments")
    );
